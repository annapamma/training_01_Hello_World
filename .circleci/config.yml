version: 2.1

commands:
  sayhello:
    description: "A very simple command for demonstration purposes"
    parameters:
      expected-changes:
        type: string
        default: "False"
      to:
        type: string
        default: "no one"
    steps:
      - run: |
          if [ ! expected-changes ]; then
              circleci-agent step halt
          fi
      - run: echo << parameters.to >>

jobs:
  build:
    docker:
      - image: "circleci/node:9.6.1"
    steps:
      - sayhello:
          to: "Lev"

