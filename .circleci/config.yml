version: 2
jobs:
  build:
    docker:
      - image: circleci/node
    resource_class: small
    steps:
      - checkout
      - run: |
          echo $CIRCLE_BUILD_NUM
  step2:
    docker:
      - image: circleci/node
    resource_class: medium
    steps:
      - checkout
      - run: |
          echo $CIRCLE_WORKFLOW_ID

workflows:
  version: 2
  test:
    jobs:
        - build
        - step2
#