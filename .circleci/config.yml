version: 2.1

executors:
  linux:
    docker:
      - image: circleci/android:api-29-node

jobs:
  run-tests:
    executor: linux
    steps:
      - run: echo 'hello world'

workflows:
  test:
    jobs:
      - run-tests
