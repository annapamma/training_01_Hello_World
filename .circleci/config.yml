version: 2
jobs:
  build:
    docker:
      - image: circleci/openjdk@sha256:9382a070f79220a7474f01969240427b0a1df805ffee02c082cacd1ba4d1d636
    steps:
      - checkout
      - run: |
          curl -H 'Authorization: token ${PERSONAL_ACCESS_TOKEN}' \
               -H 'Accept: application/vnd.github.VERSION.patch' \
               https://api.github.com/repos/annapamma/training_01_Hello_World/pulls/2
          echo "hello world" > pr.txt
      - run: cat pr.txt
      - store_artifacts:
          path: pr.txt
          destination: pr-file-changes

workflows:
  version: 2
  test:
    jobs:
        - build:
            context: remotecachedemo