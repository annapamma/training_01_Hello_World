version: 2.1

jobs:
  build:
    docker:
      - image: "circleci/node:9.6.1"
    steps:
      - run: echo "export FOO='BAR'"> '/tmp/load_change_status.sh'
      - run: source /tmp/load_change_status.sh
      - run: echo ${FOO}
      - run: cat /tmp/load_change_status.sh

