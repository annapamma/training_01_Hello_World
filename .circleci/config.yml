version: 2
jobs:
  build:
    docker:
      - image: circleci/node
    steps:
      - checkout
      - run: |
          echo $CIRCLE_BUILD_NUM

workflows:
  version: 2
  test:
    jobs:
        - build:
            context: remotecachedemo
