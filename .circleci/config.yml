version: 2.0

jobs:
  build:
    docker:
      - image: "circleci/node:9.6.1"
    steps:
      - run: echo 'hello world'
  required_check:
    docker:
      - image: "circleci/node:9.6.1"
    steps:
      - run: echo 'required check'
  test_from_api:
    docker:
      - image: "circleci/node:9.6.1"
    steps:
      - run: echo 'triggered from api'

workflows:
  version: 2
  test:
    jobs:
      - build
      - required_check
